StoreTech
=========

Simple Next.js + TypeScript store demo.

- Features: product listing (fakestoreapi), product details, cart, wishlist, checkout.
- Run: `npm install` then `npm run dev`.
- Notes: data comes from fakestoreapi; cart/wishlist persist in localStorage.

Built quick with Tailwind + Radix UI.
